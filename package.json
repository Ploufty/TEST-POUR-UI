{
  "name": "apps1d76",
  "private": true,
  "description": "Page d'accueil des outils numériques - Mission Numérique Éducatif 76",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "ajouter": "node scripts/ajouter-outil.mjs",
    "verifier": "node scripts/build.mjs --verifier",
    "build": "node scripts/build.mjs",
    "apercu": "node scripts/build.mjs && npx --yes http-server public -p 8080 -c-1 -o"
  },
  "devDependencies": {
    "esbuild": "^0.25.12"
  }
}
